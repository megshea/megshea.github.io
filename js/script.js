function card(p){return `<a class="card ${p.size}" href="project.html?project=${p.slug}"><div class="image"><img src="${p.image}" alt="${p.title}"></div><div class="meta"><div><h2>${p.title}</h2><small>${p.type}</small></div><span>↗</span></div></a>`}
const f=document.getElementById("featured-grid");if(f)f.innerHTML=projects.filter(p=>p.featured).map(card).join("");
const a=document.getElementById("all-projects");if(a){const render=x=>a.innerHTML=x.map(card).join("");render(projects);document.querySelectorAll(".filter").forEach(b=>b.onclick=()=>{document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");render(b.dataset.filter==="all"?projects:projects.filter(p=>p.category===b.dataset.filter))})}
const d=document.getElementById("project-detail");if(d){const p=projects.find(x=>x.slug===new URLSearchParams(location.search).get("project"));if(p){d.innerHTML=`<section class="project-hero"><p class="eyebrow">${p.type}</p><h1>${p.title}</h1><p>${p.description}</p></section><div class="detail-image"><img src="${p.image}" alt="${p.title}"></div><section class="placeholder-copy"><p class="eyebrow">PROJECT CONTENT</p><h2>YOUR IMAGES, VIDEO, PROCESS, AND STORY GO HERE.</h2><p>Replace this placeholder with the actual project story, additional images, video, process work, links, or credits.</p></section>`}else d.innerHTML="<h1>PROJECT NOT FOUND.</h1>"}